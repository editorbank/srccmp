{
  "src_mask": "(?i)^(.*)(\\.py)$",
  "path_replaces": [
    {"from":"\\\\", "to":"/"}
  ],
  "line_replaces": [
    {"from":"^\\s*import\\s+.*$", "to":""},
    {"from":"^\\s+|\\s+$", "to":""}
  ]
}