{
  "src_mask": "(?i)^(.*)(\\.ts|\\.tsx)$",
  "path_replaces": [
    {"from":"\\\\", "to":"/"}
  ],
  "line_replaces": [
    {"from":"^\\s*from\\s+.*$", "to":""},
    {"from":"^\\s*import\\s+.*$", "to":""},
    {"from":"^\\s+|\\s+$", "to":""}
  ]
}