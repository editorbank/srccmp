{
  "src_mask": "(?i)^(.*)(\\.cs|\\.xaml)$",
  "path_replaces": [
    {"from":"\\\\", "to":"/"}
  ],
  "line_replaces": [
    {"from":"^\\s*using\\s+.*$", "to":""},
    {"from":"^\\s*namespace\\s+.*$", "to":""},
    {"from":"^\\s+|\\s+$", "to":""}
  ]
}